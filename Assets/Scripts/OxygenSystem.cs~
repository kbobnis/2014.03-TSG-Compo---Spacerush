using UnityEngine;
using System.Collections;

public class OxygenSystem : TileSystem {
	
	public bool isBroken;
	
	// Use this for initialization
	void Start () {
		InvokeRepeating("FillWithOxy", 1.0, 1.0);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
